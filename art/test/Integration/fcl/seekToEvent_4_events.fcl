services.SeekToEventClient.nextEventsToProcess: [
   [1,0,3],
   [1,0,2],
   [1,0,4],
   [1,0,1] // Repeat first event for testing reasons.  Since the last entry in
           // this list is not the last event in the file, the event-processor will
           // resume from the last specified event.
]
