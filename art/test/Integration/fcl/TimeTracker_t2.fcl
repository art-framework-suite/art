#include "TimeTracker_t1.fcl"

# The following removes the 'scheduler' table and overwrites the
# 'TimeTracker' configuration to execute default behavior except for
# the 'printSummary' flag.
services: {
  TimeTracker: {
    printSummary: false
  }
}

process_name: TimingServiceTest2
