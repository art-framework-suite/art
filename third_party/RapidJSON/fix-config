#!/bin/bash

rel="$1"
incdir="$2"
in="$3"
out="$4"

sed -E \
  -e 's&^([[:space:]]*set[[:space:]]*\([[:space:]]*RapidJSON_SOURCE_DIR[[:space:]]+")[^"]+&\1${CMAKE_CURRENT_LIST_DIR}/'"$rel"'&' \
  -e 's&/include\b&/'"$incdir"'&' \
  "$in" > "$out"
